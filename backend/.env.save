# Database Configuration
DATABASE_URL=sqlite:///./trading_bot.db

# Redis Configuration
REDIS_URL=redis://redis:6379

# API Configuration
API_HOST=0.0.0.0
 git -C /home/MuhammedBesir/trading-botu add api/requirements.txt requirements.txt frontend/index.html 2>&1; git -C /home/MuhammedBesir/trading-botu commit -m "fix: Add Anthropic SDK dependency and favicon" 2>&1; git -C /home/MuhammedBesir/trading-botu push 2>&1
API_PORT=8000
API_RELOAD=True

# CORS Settings
CORS_ORIGINS=http://localhost:5173,http://localhost:3000,http://localhost

# Trading Settings
DEFAULT_TICKER=TRALT.IS
DEFAULT_INTERVAL=5m
DEFAULT_PERIOD=1d

# Risk Management
MAX_RISK_PER_TRADE=2.0
MAX_DAILY_LOSS=5.0
MAX_POSITIONS=5

# Caching
CACHE_TTL_REALTIME=60
CACHE_TTL_HISTORICAL=3600

# Rate Limiting
RATE_LIMIT_ENABLED=True
RATE_LIMIT_PER_MINUTE=60
RATE_LIMIT_PER_HOUR=1000
RATE_LIMIT_BURST=10

# Security
SECRET_KEY=change-this-to-a-secure-random-string-in-production
JWT_ALGORITHM=HS256
JWT_EXPIRATION_HOURS=24

# Notifications (Optional)
EMAIL_ENABLED=False
TELEGRAM_ENABLED=False
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=

# Logging
LOG_LEVEL=INFO

# AI - Claude API
# Tam API anahtarınızı buraya yapıştırın (sk-ant-api03-... ile başlayan)
ANTHROPIC_API_KEY=
 git add api/requirements.txt requirements.txt frontend/index.html && git commit -m "fix: Add Anthropic SDK and favicon fallback" && git push
 git status
 q
 git add -A && git status
 git diff --cached --name-only

